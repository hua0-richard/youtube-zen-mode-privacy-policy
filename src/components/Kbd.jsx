/* The shortcut, as three keys. Written once because it appears on two pages
   and a chord split differently in two places reads as two chords. */
export function Shortcut() {
  return (
    <>
      <kbd>Alt</kbd> <kbd>Shift</kbd> <kbd>Y</kbd>
    </>
  );
}
